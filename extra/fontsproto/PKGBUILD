pkgname=fontsproto
pkgver=2.1.1
pkgrel=1
category=x11-fonts
pkgdesc="X11 Fonts extension wire protocol"
arch=('any')
url="http://xorg.freedesktop.org/"
license=('custom')
makedepends=('xorg-util-macros')

build() {

  cd "$portsdir/$category/$pkgname"
  make clean
  make 
  make deinstall
  make package PACKAGES="$srcdir"
}

package() {
  cd $pkgdir
  tar xpf "${srcdir}/${category}/${pkgname}-${pkgver}.txz"
}


